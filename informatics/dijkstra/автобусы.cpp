//
// Created by alantheknight on 09.10.2021.
//

#include <bits/stdc++.h>
using namespace std;

int main() {

    int n;
    vector<vector<pair<int, int>>> g(n);
    cin >> n;
    int start, finish;
    cin >> start >> finish;
    start--; finish--;
    int m;
    cin >> m;
    for (int i =0 ; i < m; i++) {
        int from, t1, to, t2;
        cin >> from >> t1 >> to >> t2;

    }
}

